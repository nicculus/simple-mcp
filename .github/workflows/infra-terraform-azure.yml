name: Terraform Azure

on:
  push:
    branches: [main]
    paths:
      - "infra/terraform/environments/azure-dev/**"
      - "infra/terraform/modules/mcp-server-azure/**"
      - ".github/workflows/infra-terraform-azure.yml"
  pull_request:
    branches: [main]
    paths:
      - "infra/terraform/environments/azure-dev/**"
      - "infra/terraform/modules/mcp-server-azure/**"
      - ".github/workflows/infra-terraform-azure.yml"
  workflow_dispatch:

concurrency:
  group: azure-deploy
  cancel-in-progress: false

permissions:
  id-token: write
  contents: read
  pull-requests: write

env:
  TF_VERSION: "1.12.0"
  WORKING_DIR: infra/terraform/environments/azure-dev
  ARM_USE_OIDC: "true"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  check-changes:
    name: Check changed paths
    runs-on: ubuntu-latest
    outputs:
      terraform: ${{ steps.filter.outputs.terraform }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            terraform:
              - 'infra/terraform/environments/azure-dev/**'
              - 'infra/terraform/modules/mcp-server-azure/**'
              - 'infra/terraform/bootstrap-azure/**'
              - '.github/workflows/infra-terraform-azure.yml'

  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.outputs.terraform == 'true' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # --- Static analysis -----------------------------------------------
      # These need no cloud access. They run before OIDC auth so that every
      # PR still gets fmt/checkov feedback even when cloud credentials are
      # unavailable (e.g. before bootstrap, or on a fork).
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform fmt -check -recursive

      - name: Run Checkov
        run: |
          pip install checkov -q
          checkov \
            -d infra/terraform/bootstrap-azure \
            -d infra/terraform/environments/azure-dev \
            -d infra/terraform/modules/mcp-server-azure \
            --framework terraform --quiet --soft-fail \
            --skip-check CKV_AZURE_206 \
            --skip-check CKV_AZURE_190 \
            --skip-check CKV_AZURE_59 \
            --skip-check CKV_AZURE_33 \
            --skip-check CKV2_AZURE_1 \
            --skip-check CKV2_AZURE_18 \
            --skip-check CKV2_AZURE_21 \
            --skip-check CKV_AZURE_165 \
            --skip-check CKV_AZURE_233 \
            --skip-check CKV_AZURE_167 \
            --skip-check CKV_AZURE_166 \
            --skip-check CKV_AZURE_163 \
            --skip-check CKV_AZURE_237 \
            --skip-check CKV_AZURE_139 \
            --skip-check CKV_AZURE_164 \
            --skip-check CKV2_AZURE_5 \
            --skip-check CKV_AZURE_110 \
            --skip-check CKV_AZURE_42 \
            --skip-check CKV_AZURE_189 \
            --skip-check CKV_AZURE_109 \
            --skip-check CKV_AZURE_41 \
            --skip-check CKV_AZURE_114 \
            --skip-check CKV2_AZURE_33 \
            --skip-check CKV2_AZURE_40 \
            --skip-check CKV2_AZURE_41 \
            --skip-check CKV2_AZURE_47
          # Storage: LRS/no-queue-log/public-blob — dev TF state bucket only
          # Storage v2: private endpoint, shared-key auth, SAS expiry, anon blob — use RBAC not SAS; private endpoint is overkill for dev
          # ACR: geo-replication, zone redundancy, scanning, content trust — require Premium SKU
          # Key Vault: purge protection off, public access, no firewall, no expiry — intentional dev tradeoffs

      # --- Cloud-authenticated steps -------------------------------------
      - name: Azure Login (OIDC)
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: |
          terraform init \
            -backend-config="storage_account_name=${{ secrets.AZURE_STATE_STORAGE_ACCOUNT }}" \
            -backend-config="subscription_id=${{ secrets.AZURE_SUBSCRIPTION_ID }}" \
            -backend-config="resource_group_name=mcp-infra-bootstrap"

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.WORKING_DIR }}
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_acr_login_server: ${{ secrets.AZURE_ACR_LOGIN_SERVER }}
          TF_VAR_alarm_email: ${{ vars.TF_VAR_ALARM_EMAIL }}
        run: terraform plan -no-color -out=tfplan
        continue-on-error: true

      - name: Comment plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            const output = `#### Azure Terraform Plan \`${{ steps.plan.outcome }}\`
            <details><summary>Show Plan</summary>

            \`\`\`\n${{ steps.plan.outputs.stdout }}\`\`\`

            </details>`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        working-directory: ${{ env.WORKING_DIR }}
        env:
          ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          TF_VAR_acr_login_server: ${{ secrets.AZURE_ACR_LOGIN_SERVER }}
          TF_VAR_alarm_email: ${{ vars.TF_VAR_ALARM_EMAIL }}
        run: terraform apply -auto-approve tfplan
