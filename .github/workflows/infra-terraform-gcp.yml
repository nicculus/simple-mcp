name: Terraform GCP

on:
  push:
    branches: [main]
    paths:
      - "infra/terraform/environments/gcp-dev/**"
      - "infra/terraform/modules/mcp-server-gcp/**"
      - ".github/workflows/infra-terraform-gcp.yml"
  pull_request:
    branches: [main]
    paths:
      - "infra/terraform/environments/gcp-dev/**"
      - "infra/terraform/modules/mcp-server-gcp/**"
      - ".github/workflows/infra-terraform-gcp.yml"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  pull-requests: write

env:
  TF_VERSION: "1.12.0"
  WORKING_DIR: infra/terraform/environments/gcp-dev
  GCP_REGION: us-central1
  GCP_PROJECT_ID: mcp-infra-gcp
  GCP_STATE_BUCKET: mcp-infra-tfstate-mcp-infra-gcp

jobs:
  check-changes:
    name: Check changed paths
    runs-on: ubuntu-latest
    outputs:
      terraform: ${{ steps.filter.outputs.terraform }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            terraform:
              - 'infra/terraform/environments/gcp-dev/**'
              - 'infra/terraform/modules/mcp-server-gcp/**'
              - 'infra/terraform/bootstrap-gcp/**'
              - '.github/workflows/infra-terraform-gcp.yml'

  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    needs: check-changes
    if: needs.check-changes.outputs.terraform == 'true' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # --- Static analysis -----------------------------------------------
      # These need no cloud access. They run before OIDC auth so that every
      # PR still gets fmt/checkov feedback even when cloud credentials are
      # unavailable (e.g. before bootstrap, or on a fork).
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v4
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Format Check
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform fmt -check -recursive

      - name: Run Checkov
        run: |
          pip install checkov -q
          checkov \
            -d infra/terraform/bootstrap-gcp \
            -d infra/terraform/environments/gcp-dev \
            -d infra/terraform/modules/mcp-server-gcp \
            --framework terraform --quiet --soft-fail \
            --skip-check CKV_GCP_125 \
            --skip-check CKV_GCP_84 \
            --skip-check CKV_GCP_49 \
            --skip-check CKV_GCP_41
          # CKV_GCP_125: OIDC attribute_condition already restricts to this repo; adding ref filter breaks PRs
          # CKV_GCP_84: CSEK for Artifact Registry is operationally complex; Google-managed keys sufficient for dev
          # CKV_GCP_49: serviceAccountAdmin/User required for CI to manage Cloud Run service account via Terraform
          # CKV_GCP_41: serviceAccountUser at project level required for CI to act as Cloud Run service account

      # --- Cloud-authenticated steps -------------------------------------
      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}

      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          terraform init \
            -backend-config="bucket=${{ env.GCP_STATE_BUCKET }}"

      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          terraform plan \
            -var="gcp_project_id=${{ env.GCP_PROJECT_ID }}" \
            -var="alarm_email=${{ vars.TF_VAR_ALARM_EMAIL }}" \
            -no-color -out=tfplan
        continue-on-error: true

      - name: Comment plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v9
        with:
          script: |
            const output = `#### GCP Terraform Plan \`${{ steps.plan.outcome }}\`
            <details><summary>Show Plan</summary>

            \`\`\`\n${{ steps.plan.outputs.stdout }}\`\`\`

            </details>`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });

      - name: Terraform Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform apply -auto-approve tfplan
