# cli

Reserved for platform/OS-specific CLI distributions (e.g. an EKS-targeted
installer, PowerShell, Bash). The language clients under `client/` already
ship their own CLI binaries (`mcp-client` via npm and PyPI) — this folder is
for wrapper tooling on top of those, not a third implementation.

Nothing here yet.
