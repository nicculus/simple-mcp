# fastmcp-slim 4.x resolves the MCP SDK to mcp 2.x, which speaks the
# 2026-07-28 stateless spec (no initialize handshake, no Mcp-Session-Id,
# header-based routing). As of this pin, 4.x has no stable release yet --
# 4.0.0b3 is a BETA, pinned deliberately as the server-side half of the
# 2026-07-28 migration (server first, then both clients together; see
# docs/mcp-2026-07-28-migration.md). Move off the beta once 4.0.0 stable
# ships upstream.
fastmcp-slim[server]>=4.0.0b3,<5.0
httpx>=0.27
starlette>=0.40
uvicorn>=0.32
